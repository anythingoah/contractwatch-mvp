/**
 * Global CSS is imported for its side effects by the Next.js root layout.
 * This declaration keeps TypeScript language services from treating that
 * supported Next.js import as an unresolved module.
 */
declare module "*.css";
